export enum E_ApiRoutes {
  api = '/api',
  users = '/api/users'
}

export enum E_DashboardRoutes {
  dashboard = '/dashboard',
  users = '/dashboard/users',
  signIn = '/sign-in',
  admin = '/admin',
  profile = '/dashboard/profile',
  news = '/dashboard/news'
}
